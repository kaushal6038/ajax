books{ 
	   *  	POST & Get -- Name,Birthdate,Country,City using ajax  
	   ** 	GET foriegn key(via its id) <-- with foreign keys problem using serializers 
	}

images { *	    Drag and drop images 
		 **	    multiple images select and upload simuntaneously
		 ***	uploading data progress bar 
		}
